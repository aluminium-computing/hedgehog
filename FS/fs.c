// Hedgehog VFS -- works with ANY file system
// currently all files are hardcoded but in future we may implement devices

#include "hedge"
#include "fs.h"
#include "HRFS_driver.h"

void HHFileOpen(char *path) { // HHFileOpen opens a file
	return;
}